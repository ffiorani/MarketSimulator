const double UPPER_PRICE_LIMIT = 10000;
const double LOWER_PRICE_LIMIT  = 0.00001;

/**
 * @brief
 * constant that controls the initial price of the book
 */
const double INITIAL_PRICE= 1.0;

/**
 * @brief
 * constant that controls the price change influenced by VWAP
 */
const double PRICE_CHANGE_FACTOR = 0.1;

/**
 * @brief
 * constant that controls the volume change influenced by volume imbalance
 */
const double VOLUME_PRICE_CHANGE_FACTOR = 0.01;
